{
  "name": "@repo/db",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "auth-db-generate": "bunx --bun @better-auth/cli@latest generate --config ../../apps/server/src/auth.ts --output ./src/schema/auth.ts --yes && bun --bun drizzle-kit generate --config=drizzle-auth.config.ts",
    "user-db-generate": "bun --bun drizzle-kit generate"
  },
  "dependencies": {
    "drizzle-orm": "catalog:"
  },
  "devDependencies": {
    "@types/bun": "^1.3.5",
    "drizzle-kit": "^0.31.5",
    "typescript": "catalog:"
  }
}
