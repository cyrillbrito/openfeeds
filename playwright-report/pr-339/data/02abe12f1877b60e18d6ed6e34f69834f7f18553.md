# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: ai-chat/fullscreen.spec.ts >> AI Chat Full Page >> /ai renders full-page chat
- Location: tests/ai-chat/fullscreen.spec.ts:15:7

# Error details

```
Test timeout of 15000ms exceeded.
```

```
Error: expect(locator).toBeVisible() failed

Locator:  locator('header.sticky')
Expected: visible
Received: hidden

Call log:
  - Expect "toBeVisible" with timeout 15000ms
  - waiting for locator('header.sticky')
    27 × locator resolved to <header class="bg-base-100 border-base-300 sticky top-0 z-10 border-b shadow lg:hidden">…</header>
       - unexpected value "hidden"

```

```yaml
- button "New chat"
- paragraph: How can I help?
- paragraph: Subscribe to feeds, organize tags, manage articles, and more.
- textbox "Ask me anything..."
- complementary:
  - link "OpenFeeds logo OpenFeeds":
    - /url: /inbox
    - img "OpenFeeds logo"
    - heading "OpenFeeds" [level=2]
  - list:
    - listitem:
      - link "Discover":
        - /url: /discover
    - listitem:
      - link "Inbox":
        - /url: /inbox
    - listitem:
      - link "Feeds":
        - /url: /feeds
    - listitem:
      - link "AI Chat":
        - /url: /ai
  - link "Tags":
    - /url: /tags
  - button "Add new tag"
  - list:
    - paragraph: No tags yet
    - link "Create Tags":
      - /url: /tags
  - button "TU Test User test-1782642597365-7sasn8@e2e-test.local"
  - text: Version 0.17.0
- complementary "AI Chat":
  - button "New chat"
  - button "Expand to full page"
  - button "Close"
  - paragraph: How can I help?
  - paragraph: Subscribe to feeds, organize tags, manage articles, and more.
  - textbox "Ask me anything..."
```

# Test source

```ts
  1  | import { expect, test } from '../../fixtures/auth-fixture';
  2  | import { AiChat } from '../../lib/AiChat';
  3  | 
  4  | test.describe('AI Chat Full Page', () => {
  5  |   // Anthropic key is unavailable on forked PRs (secrets aren't passed through).
  6  |   // The `/api/chat` route returns 503 without it, so every assertion would fail.
  7  |   test.skip(!process.env.ANTHROPIC_API_KEY, 'ANTHROPIC_API_KEY not configured');
  8  |   test.slow();
  9  |   let chat: AiChat;
  10 | 
  11 |   test.beforeEach(async ({ page, user }) => {
  12 |     chat = new AiChat(page);
  13 |   });
  14 | 
  15 |   test('/ai renders full-page chat', async ({ page }) => {
  16 |     await chat.goto();
  17 | 
  18 |     await expect(page).toHaveURL('/ai');
> 19 |     await expect(chat.getPageHeader()).toBeVisible({ timeout: 15_000 });
     |                                        ^ Error: expect(locator).toBeVisible() failed
  20 |     await expect(chat.getTextarea()).toBeVisible();
  21 |     await expect(chat.getEmptyState()).toBeVisible();
  22 |   });
  23 | 
  24 |   test('FAB is hidden on /ai route', async () => {
  25 |     await chat.goto();
  26 |     await expect(chat.getFab()).not.toBeVisible();
  27 |   });
  28 | 
  29 |   test('sidebar AI Chat link navigates to /ai', async ({ page }) => {
  30 |     await page.goto('/');
  31 | 
  32 |     await page.getByRole('link', { name: 'AI Chat' }).click();
  33 |     await expect(page).toHaveURL('/ai');
  34 |   });
  35 | });
  36 | 
```