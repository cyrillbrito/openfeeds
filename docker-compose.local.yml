# Local development: build and run all services from local Dockerfiles
# Use this to test the full containerized stack locally
#
# Pre-build steps (all apps use packaging-only Dockerfiles):
#   Web:      bun run build --filter=@repo/web
#   Worker:   turbo prune @repo/worker && cd out && bun install && bun run build
#   Migrator: turbo prune @repo/migrator && cd out && bun install
services:
  postgres:
    image: postgres:18.1-alpine
    ports:
      - '5432:5432'
    environment:
      POSTGRES_DB: openfeeds
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - openfeeds-network
    restart: unless-stopped
    command:
      - postgres
      - -c
      - listen_addresses=*
      - -c
      - wal_level=logical # Required for Electric SQL
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U postgres -d openfeeds']
      interval: 5s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    ports:
      - '6379:6379'
    volumes:
      - redis_data:/data
    networks:
      - openfeeds-network
    restart: unless-stopped
    command: redis-server --save 60 1 --loglevel warning

  # Electric SQL sync service
  electric:
    image: electricsql/electric:latest
    ports:
      - '3060:3000'
    environment:
      DATABASE_URL: postgresql://postgres:postgres@postgres:5432/openfeeds?sslmode=disable
      ELECTRIC_INSECURE: true
    networks:
      - openfeeds-network
    depends_on:
      postgres:
        condition: service_healthy
    restart: unless-stopped

  migrator:
    build:
      context: .
      dockerfile: apps/migrator/Dockerfile
    environment:
      - DATABASE_URL=postgresql://postgres:postgres@postgres:5432/openfeeds
    networks:
      - openfeeds-network
    depends_on:
      postgres:
        condition: service_healthy
    restart: 'no'

  web:
    build:
      context: .
      dockerfile: apps/web/Dockerfile
    ports:
      - '3200:80'
    environment:
      - BASE_URL=${BASE_URL:-http://localhost:3200}
      - DATABASE_URL=postgresql://postgres:postgres@postgres:5432/openfeeds
      - REDIS_HOST=redis
      - BETTER_AUTH_SECRET=${BETTER_AUTH_SECRET:-dev-secret-change-me}
      - TRUSTED_ORIGINS=${TRUSTED_ORIGINS:-http://localhost:3200}
      - SIMPLE_AUTH=${SIMPLE_AUTH:-true}
      - ELECTRIC_URL=http://electric:3000
      - POSTHOG_PUBLIC_KEY=$POSTHOG_PUBLIC_KEY
      - UNREAL_SPEECH_API_KEY=$UNREAL_SPEECH_API_KEY
      - TTS_DEFAULT_VOICE=$TTS_DEFAULT_VOICE
      - DATA_PATH=${DATA_PATH:-/data}
    volumes:
      - web_data:${DATA_PATH:-/data}
    depends_on:
      redis:
        condition: service_started
      postgres:
        condition: service_healthy
      electric:
        condition: service_started
      migrator:
        condition: service_completed_successfully
    networks:
      - openfeeds-network
    restart: unless-stopped

  worker:
    build:
      context: .
      dockerfile: apps/worker/Dockerfile
    environment:
      - DATABASE_URL=postgresql://postgres:postgres@postgres:5432/openfeeds
      - REDIS_HOST=redis
      - POSTHOG_PUBLIC_KEY=$POSTHOG_PUBLIC_KEY
      - RESEND_API_KEY=$RESEND_API_KEY
      - WORKER_CONCURRENCY_ORCHESTRATOR=$WORKER_CONCURRENCY_ORCHESTRATOR
      - WORKER_CONCURRENCY_FEED_SYNC=$WORKER_CONCURRENCY_FEED_SYNC
      - WORKER_CONCURRENCY_FEED_DETAILS=$WORKER_CONCURRENCY_FEED_DETAILS
      - WORKER_CONCURRENCY_AUTO_ARCHIVE=$WORKER_CONCURRENCY_AUTO_ARCHIVE
    depends_on:
      redis:
        condition: service_started
      postgres:
        condition: service_healthy
      migrator:
        condition: service_completed_successfully
    networks:
      - openfeeds-network
    restart: unless-stopped

  # Queue management UI
  queuedash:
    image: ghcr.io/alexbudure/queuedash:3.14.0
    ports:
      - '3009:3000'
    environment:
      - QUEUES_CONFIG_JSON={"queues":[{"name":"feed-sync-orchestrator","displayName":"Feed Sync Orchestrator","type":"bullmq","connectionUrl":"redis://redis:6379"},{"name":"single-feed-sync","displayName":"Single Feed Sync","type":"bullmq","connectionUrl":"redis://redis:6379"},{"name":"feed-detail","displayName":"Feed Details","type":"bullmq","connectionUrl":"redis://redis:6379"},{"name":"auto-archive","displayName":"Auto Archive","type":"bullmq","connectionUrl":"redis://redis:6379"}]}
    depends_on:
      - redis
    networks:
      - openfeeds-network
    restart: unless-stopped

volumes:
  postgres_data:
    driver: local
  redis_data:
    driver: local
  web_data:
    driver: local

networks:
  openfeeds-network:
    driver: bridge
