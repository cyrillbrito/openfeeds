# Required for server-side auth (TanStack Start)
CLIENT_DOMAIN=http://localhost:3000
BETTER_AUTH_SECRET=<RANDOM_STRING>
SIMPLE_AUTH=true

# Database path (relative to apps/web or absolute)
DB_PATH=../../dbs

# Optional: Redis for BullMQ (if using queues)
REDIS_HOST=localhost
REDIS_PORT=6379

# Optional: TTS Configuration (for article audio)
# Get API key from https://unrealspeech.com/
UNREAL_SPEECH_API_KEY=
# Available voices: Scarlett, Dan, Liv, Will, Amy
TTS_DEFAULT_VOICE=Scarlett
