{
  "name": "@repo/marketing",
  "type": "module",
  "scripts": {
    "dev": "bun --env-file=../../.env astro dev",
    "build": "bun --env-file=../../.env astro build",
    "preview": "bun --env-file=../../.env astro build && wrangler pages dev",
    "astro": "astro",
    "deploy": "bun --env-file=../../.env astro build && wrangler pages deploy",
    "cf-typegen": "wrangler types"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^12.6.13",
    "@astrojs/solid-js": "^5.1.3",
    "@tailwindcss/vite": "^4.2.2",
    "astro": "^5.18.1",
    "resend": "^6.9.4",
    "solid-js": "^1.9.12",
    "tailwindcss": "^4.2.2"
  },
  "devDependencies": {
    "wrangler": "^4.78.0"
  }
}
