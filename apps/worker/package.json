{
  "name": "@repo/worker",
  "main": "src/index.ts",
  "type": "module",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "start": "bun run src/index.ts",
    "check-types": "tsc --noEmit",
    "benchmark": "bun run src/benchmark/index.ts",
    "docker:publish": "docker build -f ./Dockerfile -t ghcr.io/cyrillbrito/openfeeds-worker:latest ../.. && docker push ghcr.io/cyrillbrito/openfeeds-worker:latest"
  },
  "dependencies": {
    "@repo/db": "workspace:*",
    "@repo/domain": "workspace:*",
    "@repo/readability": "workspace:*",
    "@repo/shared": "workspace:*",
    "bullmq": "^5.67.1",
    "drizzle-orm": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@types/bun": "^1.3.6",
    "better-auth": "catalog:",
    "typescript": "catalog:"
  }
}
