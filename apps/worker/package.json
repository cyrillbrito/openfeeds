{
  "name": "@repo/worker",
  "main": "src/index.ts",
  "type": "module",
  "scripts": {
    "dev": "bun --env-file=../../.env run --hot src/index.ts",
    "start": "bun --env-file=../../.env run src/index.ts",
    "check-types": "tsc --noEmit",
    "benchmark": "bun --env-file=../../.env run src/benchmark/index.ts"
  },
  "dependencies": {
    "@repo/db": "workspace:*",
    "@repo/domain": "workspace:*",
    "@repo/readability": "workspace:*",
    "@repo/shared": "workspace:*",
    "@t3-oss/env-core": "^0.13.10",
    "bullmq": "^5.69.2",
    "drizzle-orm": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@types/bun": "^1.3.9",
    "better-auth": "catalog:",
    "typescript": "catalog:"
  }
}
